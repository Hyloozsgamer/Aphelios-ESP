const CONFIG = {
    // Twitch Channel to connect the real chat via tmi.js
    TWITCH_CHANNEL: 'espetosmaker',

    // How many messages to keep in chat before deleting old ones
    MAX_CHAT_MESSAGES: 50,
    
    // How long before chat messages fade out (in ms)
    CHAT_FADE_TIMEOUT_MS: 30000,
    
    // Enable simple mouse-parallax effect (useful for testing)
    ENABLE_PARALLAX: true
};
